import { Component } from 'react';

export default class ForecastPanel extends Component {
    constructor(props) {
        // NOTE: this is a mandatory step to initialize the parent Component!
        super(props);
    }
    render() {
        return (
            <div>Review Forecast</div>
        );
    }
}